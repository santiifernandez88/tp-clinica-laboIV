import { EstadoTextoDirective } from './estado-texto.directive';

describe('EstadoTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoTextoDirective();
    expect(directive).toBeTruthy();
  });
});
