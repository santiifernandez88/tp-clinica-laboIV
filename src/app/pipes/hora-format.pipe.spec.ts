import { HoraFormatPipe } from './hora-format.pipe';

describe('HoraFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HoraFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
