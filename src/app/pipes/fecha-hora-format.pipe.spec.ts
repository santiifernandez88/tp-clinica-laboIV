import { FechaHoraFormatPipe } from './fecha-hora-format.pipe';

describe('FechaHoraFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaHoraFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
